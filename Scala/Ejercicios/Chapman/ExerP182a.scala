
println("Capture the number to calculate its factorial: ")
var baseNumber: Int = 0

var baseNumber = readInt()
var product: Int = 1
var i: Int = 1

/*println(baseNumber)
  var baseNumber: Int = baseNumber.toInt
  println(baseNumber)
*/

def factorial(n:Int): Int = {
		while(i<=baseNumber) {
		product *= i
		i +=
	}
}

factorial(baseNumber)